// Centraliza la URL base de la API para facilitar pruebas en emulador/dispositivo
// Cambia esta URL según la red (emulador, dispositivo físico, o producción).
export const API_URL = 'http://10.0.0.143:3000'

// Export por defecto para compatibilidad con imports simples
export default API_URL
